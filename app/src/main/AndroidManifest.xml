<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.gilbercs.exemplokotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExemploKotlin">
        <activity android:name=".container.SearchViewActiivity"></activity>
        <activity android:name=".container.ExpandableActivity" />
        <activity android:name=".container.SearchViewToobar" />
        <activity android:name=".container.CustomActivity" />
        <activity android:name=".container.SearchViewActivity" />
        <activity android:name=".container.GridViewActivity" />
        <activity android:name=".container.ListViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>